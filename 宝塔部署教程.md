# 符宝 FuBao · 宝塔面板部署教程

本教程介绍如何将 FuBao（Next.js 16 全栈应用）通过宝塔面板部署到自有服务器，并以 Nginx 反向代理 + HTTPS 对外提供服务。

---

## 〇、一键安装与更新（推荐）

仓库内置两个脚本（`deploy/install.sh` / `deploy/update.sh`），可替代第一~六章的全部手动步骤。

### 一键安装（首次部署）

以 root 登录服务器执行：

```bash
curl -fsSL https://raw.githubusercontent.com/junnyjchen/fubaoltd/main/deploy/install.sh -o /root/install.sh
bash /root/install.sh --domain www.fubao.ltd
```

脚本会自动完成：

1. **环境检查与安装**：Node.js 24、pnpm、PM2、git、Nginx（缺失则自动装）
2. **拉取代码**：克隆到 `/www/wwwroot/fubao`
3. **生成环境变量**：`deploy/fubao.env`（自动生成强随机 JWT_SECRET、以域名推导 BASE_URL；已有的不覆盖）
4. **构建**：`pnpm install` + `pnpm run build`（tsup 编译自定义 server + Next.js 构建）
5. **PM2 常驻**：进程名 `fubao`，`instances: 1`（globalThis 内存态要求单实例，不可调大）
6. **Nginx 反向代理**：80 端口 → 127.0.0.1:5000，写入 `/www/server/panel/vhost/nginx/fubao.conf`（已存在则跳过，宝塔面板自建的站点配置不受影响）
7. **检测宝塔 SSL**：若面板已为该域名签发证书则自动挂载 443 并 HTTP 跳转

可选参数：

```bash
bash install.sh --domain www.fubao.ltd \
  --repo https://github.com/junnyjchen/fubaoltd.git \
  --branch main \
  --dir /www/wwwroot/fubao
```

### 一键更新（日常发版）

```bash
bash /www/wwwroot/fubao/deploy/update.sh
```

流程：`git pull` → 重新构建 → **健康检查通过才重启**（探测 `/api/blessing`，失败自动 `pm2 stop fubao` 并提示用 `git reflog` 回滚）→ 平滑重载。

健康检查失败不影响线上：新构建未通过检测时不会替换运行中的进程，回滚命令会直接打印在终端。

### 脚本假设

- 域名 `www.fubao.ltd` 已解析到服务器 IP（宝塔面板或脚本外自行完成）
- SSL 优先在宝塔面板「网站 → SSL」中申请（脚本只做检测挂载，不代签证书）
- AI 相关功能（`/api/ai/*`）依赖沙箱凭证，自部署环境默认不可用，属预期行为

### 为什么拉取代码要输账号密码？（私有仓库认证）

GitHub 自 2021-08 起已**彻底禁用账号密码**拉取/推送，只认 **Personal Access Token** 或 **SSH Key**。仓库 `junnyjchen/fubaoltd` 是私有的，所以 clone / pull 都会要求认证——无人值守的部署脚本遇到提示会直接卡死。脚本已内置免交互处理（认证缺失会立刻失败并打印指引，不会挂起），配置一次即可永久免密：

**方式 A · SSH Deploy Key（推荐，只读、一次配置）**

```bash
# 1. 服务器上生成专用密钥（无口令，供脚本免交互使用）
ssh-keygen -t ed25519 -C fubao-deploy -f /root/fubao_deploy_key -N ''

# 2. 复制公钥内容
cat /root/fubao_deploy_key.pub

# 3. GitHub 仓库 → Settings → Deploy keys → Add deploy key
#    粘贴公钥；不要勾选 "Allow write access"（部署只需只读）

# 4. 用 SSH 地址 + 密钥路径安装
REPO_URL=git@github.com:junnyjchen/fubaoltd.git \
DEPLOY_KEY=/root/fubao_deploy_key bash deploy/install.sh
```

**方式 B · Personal Access Token（HTTPS 保持不变）**

```bash
# 1. GitHub → Settings → Developer settings → Personal access tokens
#    → Fine-grained tokens → Generate new token
#    Repository access 选 fubaoltd；Permissions → Contents: Read-only

# 2. 首次安装时传入（token 自动持久化到 deploy/fubao.env，600 权限，已被 .gitignore 排除）
GITHUB_TOKEN=github_pat_xxxxxxxx bash deploy/install.sh
```

之后 `update.sh` 自动复用 `deploy/fubao.env` 里的 token / `deploy/deploy_key` 私钥，日常发版不再需要任何认证参数。

---

## 一、部署架构

```
用户浏览器 ──HTTPS──▶ 宝塔 Nginx（80/443）──反向代理──▶ Node 服务（127.0.0.1:5000）
                                                        │
                                          node dist/server.js（自定义 HTTP 服务器）
                                          内部承载 Next.js 16 生产构建（.next/）
```

- **应用入口**：`dist/server.js`（由 `src/server.ts` 经 tsup 打包的自定义服务器，非 `next start`）
- **运行端口**：由环境变量 `PORT` 决定，默认 `5000`
- **运行模式开关**：`COZE_PROJECT_ENV` —— **不等于 `PROD` 时服务器以 dev 模式运行**（慢、内存占用大）。生产部署必须设置 `COZE_PROJECT_ENV=PROD`

## 二、服务器与环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | CentOS 7+ / Ubuntu 20.04+ / Debian 11+ |
| 内存 | ≥ 2 GB（`next build` 建议可用内存 ≥ 2 GB；2G 小机请先加 swap） |
| Node.js | ≥ 20（推荐 22 LTS 或 24，宝塔"Node.js 版本管理器"安装） |
| 包管理器 | **仅限 pnpm ≥ 9**（项目 `preinstall` 含 `only-allow pnpm`，用 npm/yarn 会直接报错） |
| 进程守护 | PM2（宝塔"PM2 管理器"插件或命令行） |
| 数据库 | **无需** —— 当前数据层为内存存储 + Spree 兼容层（见"七、重要说明"） |

## 三、安装宝塔面板与运行环境

### 1. 安装宝塔面板

SSH 登录服务器，按官方命令安装（以官网为准）：

```bash
# Ubuntu/Debian
wget -O install.sh https://download.bt.cn/install/install_lts.sh && bash install.sh ed8484bec

# CentOS
yum install -y wget && wget -O install.sh https://download.bt.cn/install/install_lts.sh && sh install.sh ed8484bec
```

安装完成后记录面板地址、账号密码，登录面板。

### 2. 安装软件（宝塔软件商店）

- **Nginx**（任意稳定版，如 1.26）
- **Node.js 版本管理器** → 安装 Node **22 或 24**，并设为默认版本
- **PM2 管理器**（可选，用命令行 PM2 可跳过）

### 3. 安装 pnpm

宝塔终端（或 SSH）执行：

```bash
npm install -g pnpm@9
pnpm -v   # 确认 >= 9.0.0
```

## 四、获取代码

方式一：Git（推荐，便于后续更新）

```bash
cd /www/wwwroot
git clone <你的仓库地址> fubao
cd fubao
```

方式二：面板上传

1. 本地打包项目（**排除 `node_modules`、`.next`、`dist`**）
2. 宝塔 → 文件 → 上传到 `/www/wwwroot/fubao` 并解压

## 五、配置环境变量（关键）

> **注意：代码不读取 `.env` 文件**（项目未启用 dotenv 自动加载），环境变量必须通过进程环境注入（PM2 配置 / shell 导出）。

在项目根目录创建 PM2 配置文件 `/www/wwwroot/fubao/ecosystem.config.js`：

```js
module.exports = {
  apps: [
    {
      name: 'fubao',
      cwd: '/www/wwwroot/fubao',
      script: 'dist/server.js',
      env: {
        NODE_ENV: 'production',
        COZE_PROJECT_ENV: 'PROD',   // 生产模式开关（缺省则回退 dev 模式）
        PORT: '5000',                // Node 服务监听端口
        HOSTNAME: '127.0.0.1',
        JWT_SECRET: '请替换为32位以上随机字符串',  // 生产必改！默认密钥仅供开发
        NEXT_PUBLIC_BASE_URL: 'https://你的域名',
        COZE_PROJECT_DOMAIN_DEFAULT: 'https://你的域名', // OG 分享卡片基址
        // ---- 以下为可选集成，按需开启 ----
        // SPREE_API_URL: 'https://你的-spree-实例',   // 接真实 Spree 5.4 后端
        // STRIPE_SECRET_KEY: 'sk_live_xxx',           // Stripe 真实支付
        // CRYPTO_TRC20_ADDRESS: 'T...',               // USDT TRC20 收款地址
        // CRYPTO_ERC20_ADDRESS: '0x...',              // USDT/USDC ERC20 收款地址
        // CRYPTO_BEP20_ADDRESS: '0x...',              // BEP20 收款地址
        // CRYPTO_SOL_ADDRESS: '...',                  // SOL 收款地址
        // CRYPTO_POLYGON_ADDRESS: '0x...',            // Polygon 收款地址
      },
      instances: 1,        // 内存态数据存于 globalThis，多实例会各自独立，务必保持 1
      exec_mode: 'fork',
      max_memory_restart: '600M',
    },
  ],
};
```

生成随机密钥：`openssl rand -hex 32`

## 六、构建与启动

### 1. 安装依赖并构建

```bash
cd /www/wwwroot/fubao
pnpm install --prefer-frozen-lockfile   # 安装依赖
pnpm next build                          # 生产构建，产出 .next/
pnpm tsup src/server.ts --format cjs --platform node --target node20 \
  --outDir dist --no-splitting --no-minify   # 打包自定义服务器，产出 dist/server.js
```

与 `scripts/build.sh` 内容一致；也可直接 `bash scripts/build.sh`。

> 内存不足导致构建失败（OOM / killed）：先加 swap
> ```bash
> dd if=/dev/zero of=/swapfile bs=1M count=2048 && chmod 600 /swapfile \
>   && mkswap /swapfile && swapon /swapfile
> ```

### 2. PM2 启动

```bash
pm2 start ecosystem.config.js
pm2 save          # 保存进程列表
pm2 logs fubao    # 查看日志，确认输出 "> Server listening at http://127.0.0.1:5000 as PROD"
```

设置开机自启：

```bash
pm2 startup       # 按提示执行输出的命令
```

### 3. 验证本地服务

```bash
curl -I http://127.0.0.1:5000   # 应返回 200 / 307
```

## 七、宝塔网站与反向代理

### 1. 创建站点

宝塔 → 网站 → 添加站点：
- 域名：`你的域名`（需已解析到服务器 IP）
- PHP 版本：纯静态（无需 PHP）
- 不创建数据库

### 2. 配置反向代理

站点 → 设置 → **反向代理** → 添加：
- 代理名称：`fubao`
- 目标 URL：`http://127.0.0.1:5000`

或直接在站点配置文件中添加（效果相同，可控性更强）：

```nginx
location / {
    proxy_pass http://127.0.0.1:5000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # SSE 流式输出（AI 助手聊天）必须关闭缓冲，否则打字机效果会被卡住
    proxy_buffering off;
    proxy_cache off;
    proxy_read_timeout 300s;   # SSE 长连接超时
}
```

### 3. 申请 SSL 证书

站点 → 设置 → **SSL** → Let's Encrypt → 勾选域名 → 申请 → 开启"强制 HTTPS"。

### 4. 防火墙与安全组

- 宝塔 → 安全 → 放行 `80`、`443`；**不要**对公网放行 5000（仅本机反代访问）
- 云厂商安全组同样只放行 80/443/SSH

## 八、部署验证清单

| 检查项 | 方法 | 预期 |
|--------|------|------|
| 首页 | `https://你的域名` | 宣纸米白风格首页正常渲染 |
| 商品列表 | `/talisman` | 商品卡片 + 分类筛选 |
| 购物车闭环 | 加购 → `/cart` | Spree 兼容层购物车正常 |
| 登录注册 | `/login` `/register` | demo@fubao.com / demo123 可登录 |
| AI 助手流式 | `/ai-chat` 提问 | 打字机式流式输出（见第七节 proxy_buffering） |
| 订单确认 | 结账完成页 | 订单号 + 明细渲染 |
| HTTPS | 地址栏 | 证书有效，强制跳转 |

## 九、常见问题

**Q1：访问 502 Bad Gateway**
`pm2 logs fubao` 查看报错；确认 `curl -I http://127.0.0.1:5000` 本机可达；确认 Nginx 反代目标端口与 `PORT` 一致。

**Q2：页面极慢、日志出现 "as development"**
未设置 `COZE_PROJECT_ENV=PROD`。修改 ecosystem.config.js 后 `pm2 restart fubao --update-env`。

**Q3：`pnpm install` 报 only-allow 错误**
说明误用了 npm/yarn。本仓库强制 pnpm（`preinstall` 钩子）。

**Q4：AI 助手 / 文件上传不可用**
这些能力（`/api/ai/*`、`/api/upload`、`/api/knowledge/*`）依赖扣子（Coze）沙箱内置凭证，自建服务器上默认无法调用，属预期行为；站点其余功能不受影响。如需启用，请替换为自有的 LLM/对象存储凭证并改造对应路由。

**Q5：重启后订单、注册用户、钱包余额不见了？**
当前订单/用户/钱包/优惠券领取等数据存于**进程内存（globalThis）**，重启即回到种子数据。这是演示态设计。生产化路径：设置 `SPREE_API_URL` 指向真实 Spree Commerce 5.4 实例——商品、购物车、订单、结账全链路会自动切换到远端 Spree，无需改代码。

**Q6：Stripe 加密支付如何启用？**
支付方式列表由 `/api/checkout/payment_methods` 动态返回。Stripe 需配置 `STRIPE_SECRET_KEY` 并完善 `/api/checkout/payment` 的真实扣款逻辑（当前为演示态）。加密支付收款地址通过 `CRYPTO_*_ADDRESS` 环境变量注入，未配置时使用占位地址。

## 十、更新部署

```bash
cd /www/wwwroot/fubao
git pull
pnpm install --prefer-frozen-lockfile
pnpm next build
pnpm tsup src/server.ts --format cjs --platform node --target node20 \
  --outDir dist --no-splitting --no-minify
pm2 restart fubao --update-env
```

> 注意：重启会清空内存态数据（见 Q5），选择低峰期操作。

## 十一、安全建议

- **必改 `JWT_SECRET`**（默认值为公开的弱密钥，仅限开发）
- 宝塔面板：修改默认端口、绑定访问域名、开启面板 SSL、限制登录 IP
- SSH：禁用 root 密码登录，改用密钥
- 定期 `pm2 logs` 巡检异常请求
